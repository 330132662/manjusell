// 全局配置
// TODO: 替换为你的真实后端域名（微信小程序需在后台配置 request 合法域名）
export const BASE_URL = 'https://your-api.example.com/api'

// 后端未就绪时设为 true，接口直接返回本地假数据，方便先看 UI；
// 接入真实后端后改为 false 即可走真实网络请求。
export const USE_MOCK = true
