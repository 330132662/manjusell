# 2026-09-05

## 微信 H5 用户中心单文件页面
- 交付 `user-center.html`：零依赖原生 JS 单文件，移动端优先，微信内置浏览器为主场景。
- 三态切换：非微信浏览器提示 / 微信授权登录页 / 用户中心；URL 带 token 参数时存 localStorage 并 replaceState 清参。
- 接口：`/wxapp/wxlogin`(整页跳转 back 参数) / `myProfile` / `myOrders?page&page_size`；token 失效检测(401 或 message 含 token/登录/授权/失效)→清 localStorage→回登录页。
- 金额单位【分】÷100；source_nickname 空显示"店铺直购"；物流单号可复制；滚动到底自动加载下一页；空态"去视频号橱窗逛逛吧"。
- 配置区顶部：API_BASE / BRAND_NAME / BRAND_LOGO / PRIMARY(默认 #07C160)。
