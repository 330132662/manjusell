# 2026-09-04

## sddy（鼎耀系统用户端 / uni-app Vue3）— 微信授权登录 + 订单页
- 在 `pages/user/user.vue` 实现「微信授权登录 → 拉取 openid → 展示订单列表」页（原文件为占位空壳）。
- 新建基础设施：`utils/config.js`（BASE_URL + USE_MOCK 开关）、`utils/request.js`（uni.request 封装，约定后端返回 `{code:0,data,...}`，自动带 Bearer token）、`api/index.js`（wechatLogin / getOrderList，含 MOCK 假数据）。
- pages.json 给 `pages/user/user` 补导航标题「个人中心」。
- 后端契约：POST `/auth/wechat-login` {code} → {openid,token?,nickname?,avatar?}；GET `/order/list`（带 token）→ {list:[{order_no,title,amount,status,create_time}]}。接入真实后端时把 `utils/config.js` 的 BASE_URL 改成真实域名、USE_MOCK 改 false。
- 注意：mp-weixin 用 `uni.login({provider:'weixin'})` 拿 code；H5/App 需走微信网页授权 OAuth 重定向拿 code（代码已用条件编译留 TODO）。
